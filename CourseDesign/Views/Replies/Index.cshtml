@model IEnumerable<CourseDesign.Models.Reply>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReplyContent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReplyTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReplyUserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReplyContent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReplyTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReplyUserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReplyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReplyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReplyID })
        </td>
    </tr>
}

</table>

<div>
    @Html.ActionLink("Back to List", "Index", "Messages")
</div>
<div>
    @Html.ActionLink("Logout", "Index", "Users")
</div>
